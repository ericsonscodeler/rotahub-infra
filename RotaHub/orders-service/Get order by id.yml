info:
  name: Get order by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{ordersBaseUrl}}/orders/{{orderId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
