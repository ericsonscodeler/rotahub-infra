info:
  name: List orders
  type: http
  seq: 3

http:
  method: GET
  url: "{{ordersBaseUrl}}/orders?page=0&size=20"
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "20"
      type: query
    - name: status
      value: CREATED
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
